# 变更记录

本文件从 2026-09-14 开始维护。按记录时间倒序追加；日期使用项目本地时间。功能编号见 [功能索引](features.md)。基线盘点不代表新增发布；没有证据的历史不补造。



## CHG-20260915-03 — 从 1.0.0 开始发布正式版本

- 功能：F12。
- 原因：用户指定从 1.0.0 开始编号，并发布 Release 与 Git 标签。
- 前后变化：0.1.0 开发版本 → 1.0.0；设置、应用元数据与锁文件同步，发行包使用版本化文件名并提供 SHA-256 校验值。
- 涉及文件：`package.json`、`package-lock.json`、`docs/CHANGELOG.md`，以及由构建脚本生成的发行物；本次提交同时收录设置版本展示和统一打包版本来源的改动。
- 边界：发布到当前项目仓库，标签为 `v1.0.0`；仅提供 macOS Apple Silicon 构建，未进行 Apple 签名或公证，不包含用户数据。
- 验证：构建及 81 项测试通过，源码和打包生成物隐私检查通过；发布附件提供 SHA-256 校验值，发布前核对应用元数据与包内版本。
- 交付：本记录随 v1.0.0 发行源码归档；发布结果以仓库 Release 和远端标签为准。
- 回退：可检出此前源码重新构建；不移动或覆盖已发布标签。


## CHG-20260915-02 — 设置展示统一版本号

- 功能：F12。
- 原因：用户需要可见的应用版本号。
- 前后变化：版本仅记录在依赖清单且打包脚本重复硬编码 → 设置顶部显示当前版本，前端与打包脚本统一读取 `package.json` 的 `version`，沿用现有 0.1.0。
- 涉及文件：`src/App.tsx`、`scripts/package.mjs`、`README.md`、`docs/features.md`、`docs/CHANGELOG.md`。
- 边界：只读版本展示，不改变用户设置、任务监测或更新机制；修改版本后需重新构建与打包。
- 验证：构建、源及生成物隐私检查通过；源码、暂存与归档清单、macOS 应用版本和构建版本均为 0.1.0；原生设置页已确认显示当前版本。
- 交付：应用包已生成并重启，版本显示已实机确认；包内记录为打包时快照，最终核对结果补记于仓库。
- 回退：移除版本展示并恢复打包脚本即可，无用户数据迁移。


## CHG-20260915-01 — 建立源码版本基线

- 功能：F12。
- 原因：用户要求提交当前项目，目录此前没有 Git 版本记录。
- 前后变化：未纳入版本控制 → 初始化本地仓库并提交当前源码、测试、维护文档与运行/打包所需图标；构建目录仅保留两个必需图标，排除依赖、应用包与 QA 预览。
- 涉及文件：`.gitignore`、本变更记录；首次提交收录项目现有源码。
- 边界：只创建本地提交，不建立远程仓库或上传；不更改应用行为或用户设置，不删除已生成的本地文件。
- 验证：源码隐私检查通过，构建通过，现有测试 81/81 通过。
- 交付：纳入本次本地首次提交；未执行远程推送。
- 回退：版本控制不改变当前应用；生成物继续由构建脚本重建。


## CHG-20260914-06 — 右上角默认位置、位置记忆与开机启动

- 功能：F10、F13。
- 原因：用户希望首次位于右上角，拖动后保留位置，并可选择登录时启动。
- 前后变化：每次固定右下角 → 首次右上角、移动后自动保存显示器及坐标；重启恢复位置，显示器重排按相对位置恢复、断开时回到主屏右上角。设置新增开机启动开关，默认不注册。
- 涉及文件：`core/pet-position.mjs`、`core/login-startup.mjs`、`electron/main.mjs`、`src/App.tsx`、`src/types.ts`，对应测试与使用说明。
- 边界：位置仅保存到 Aster 自有数据目录；登录项只在用户切换时修改 Aster 自身，实际状态从系统读取。开发运行时禁用注册。原任务和其他软件配置不变；不执行真实注销/重启电脑测试。
- 验证：位置、缩放与登录项适配共 12 项测试通过，覆盖旧值、序列化恢复、负坐标显示器、断屏、启动开关往返、系统修改和失败；构建通过。隔离的隐藏 Electron 窗口验证了默认右上角、移动事件持久化和重启位置恢复；实际应用设置页确认开机启动开关可用且默认关闭。未注销电脑测试自动登录启动，也未为测试修改用户系统登录项。
- 交付：源代码已改、构建通过，应用已打包并重启；原生设置入口及隔离原生位置往返验证通过。
- 回退：设置中关闭开机启动；位置记忆不改变任务记录。回退源改动后重新构建，无独立已验证旧包。


## CHG-20260914-06 — 清理初版角色与失效样式

- 功能：F06、F09、F12。
- 原因：当前已使用机器人界面，初版角色加载、导入存储及旧布局样式不再有入口。
- 前后变化：删除旧角色加载组件、IndexedDB 存储封装、附带模型及专用依赖；161 条样式规则/568 项声明 → 136 条规则/446 项声明，清除旧布局规则与被覆盖声明，并整理格式。
- 涉及文件：移除 `src/Avatar.tsx`、`src/model-store.ts`、`public/models/companion.vrm`；更新 `src/styles.css`、`src/App.tsx`、`src/types.ts`、`electron/main.mjs`、`electron/preload.cjs`、依赖清单与锁文件、第三方说明和功能索引；移除图标脚本未使用的导入。
- 边界：当前 3D Robot、SVG 回退及动作仍保留；不删除用户已保存的模型数据，不修改任务来源记录。删除没有发送方的旧模式订阅和没有调用方的模型刷新分支，不改变现有任务/设置 IPC。已移除模型与库的分发署名同步删除，其余仍用依赖的许可证保留。
- 验证：380×490 合成任务预览中，任务列表 204、设置 58、桌宠 70 个元素清理前后计算样式完全相同；构建通过。全量测试 72 项通过，唯一受沙箱监听限制的 IPC 用例在允许监听的环境补跑通过（IPC 共 3/3），全量 73 个用例均已获得通过结果。
- 交付：源代码已清理，统一生成的应用包已包含本次清理；归档和暂存区均无旧模型及专用依赖，包内前端资源与构建结果一致，废弃桥接已移除。生成物隐私检查 132 个文本通过，维护文档链接可解析。打包负责人已重启并完成原生显示核对，本任务未重复打包或重启。包内文档为打包时快照，最终结果以仓库记录为准。
- 回退：恢复独立模块后须重新安装对应依赖并构建；本次不迁移或清除旧 IndexedDB 数据。没有建立独立回退包，不承诺一键恢复。


## CHG-20260914-05 — 机器人增加轻快节奏与间歇招手

- 功能：F09。
- 原因：用户希望机器人表现更欢快。
- 前后变化：平缓漂浮和工作摆臂 → 待命/工作/完成增加双拍起伏与头身反向轻摆、微笑加深；待命招手间隔缩短，工作每 8 秒短暂抬手；完成时短暂连续弹跳和双臂欢呼。
- 涉及文件：`core/robot-motion.mjs`、`src/robot.css`、`tests/robot-motion.test.mjs`、`docs/features.md`、`docs/CHANGELOG.md`。
- 边界：不改变任务状态判定、数量或提醒；失败等状态仍保留原语义。减少动作时保持静态。SVG 回退同步欢快节奏，不增加模型资源或外部调用。
- 验证：动作相关 6 项测试通过，覆盖完整动作周期的视口边界、状态切换平滑、欢呼结束、间歇招手及减少动作；构建和打包隐私检查通过。
- 交付：代码和构建已完成；应用包已生成并重启；原生窗口已确认新版工作状态机器人正常显示、身体与状态条未裁切。包内记录为打包时快照，最终核对结果补记于仓库。
- 回退：恢复这三个动画与测试文件的对应调整后重新构建；无独立已验证回退包。


## CHG-20260914-04 — 刷新分发文档并保留项目规则入口

- 功能：F12。
- 原因：源码说明清理后需同步生成副本，README 新增的项目规则链接也需要在包内有目标文件。
- 前后变化：旧暂存和应用说明 → 从当前源重新构建；分发缺少项目规则文件 → 打包时一并复制 `AGENTS.md`。
- 涉及文件：`scripts/package.mjs`、`docs/features.md`、`docs/CHANGELOG.md`，以及脚本生成的构建与应用副本。
- 边界：只刷新本项目分发物；不恢复旧路径示例，不修改其他软件配置。开发索引的源码和测试链接需在源码仓库查看，应用不附带开发源码和测试。
- 验证：构建、打包前后隐私检查通过；显式生成物检查 132 个文本通过。包内存在 README 引用的项目规则与维护文档。
- 交付：应用包已生成；本次文档与分发清理没有再次重启运行中的桌宠。
- 回退：通过源文件与打包脚本重新生成；不恢复已清理的个人信息或固定路径。

## CHG-20260914-03 — 维护规则与路径隐私边界

- 功能：F12，涉及所有模块的维护约定。
- 原因：需要明确每次修改可做什么、如何定位功能、如何记录结果，并防止个人信息或固定机器路径进入项目与交付物。
- 前后变化：分散的使用说明 → 项目协作入口、维护规范、功能索引和可追溯变更记录；机器专属 CLI 示例 → 项目相对入口；固定系统命令位置 → 按名称调用；固定测试目录 → 运行时临时目录。
- 涉及文件：`AGENTS.md`、`README.md`、`docs/`，`scripts/check-privacy.mjs`、`scripts/package.mjs`、`package.json`、`index.html`，`core/local-reader-utils.mjs`、`core/task-jump.mjs` 及对应测试。
- 边界：只清理 Aster 源与由其生成的交付物，不修改其他软件数据；不在本记录复述被清理的值。API 路由、设备格式、标准解释器声明不作为安装目录。原始运行时记录仍仅在本机用于任务观察。
- 验证：源文本检查 63 个文件通过；读取、CLI、跳转相关 31 项测试与新增隐私检查 2 项通过；构建通过；6 份文档的 59 个本地链接均可解析。生成物初次检查发现旧副本；统一打包后复查通过，包含构建目录、暂存副本与应用包内文本。
- 交付：代码与文档已写入；统一打包负责人已从清理后的源刷新应用包，打包前后隐私检查通过。本条结果随后同步到仓库记录；包内文档是打包时的静态快照，以仓库最新记录为准。未为文档更新重启应用。
- 回退：维护文档可按记录修订；不要恢复被清理的信息。工具按名称调用时依赖运行环境可发现对应系统工具；不可用时走现有失败提示。没有为本条生成独立回退包。

## CHG-20260914-02 — 每个任务展示 Token 用量

- 功能：F07。
- 原因：用户需要看每个任务的 Token 消耗。
- 前后变化：卡片无用量 → 展示累计或近期 Token；悬停查看精确值；数据缺失显示未提供。
- 涉及文件：`core/token-usage.mjs`、Codex/Claude reader、`core/local-reader-utils.mjs`、`src/types.ts`、`src/App.tsx`、`src/styles.css`、`tests/token-usage.test.mjs`。
- 边界：Codex 累计字段与日志累计值相符；Claude 按消息 ID 去重，截断日志标近期；缓存包含在统计内，非费用或额度。其他来源没有记录时不虚构用量。
- 验证：当次相关 16 项测试与构建通过；已在桌面应用的任务卡片确认累计 Token 显示。具体运行值不写入维护样例。
- 交付：当次已纳入统一应用包并确认桌面显示。后续卡片布局调整以当前源码为准。
- 回退：可移除展示与可选字段读取，不更改来源统计记录；不要把缺失用量改成 0。

## CHG-20260914-01 — 主题下增加具体问题子标题

- 功能：F06。
- 原因：任务主题不足以说明本轮具体问题。
- 前后变化：只有主题 → 主题下显示当前轮次可读取的用户问题，缺内容不占空行，长文本限制两行。
- 涉及文件：`core/user-request.mjs`、`core/codex-reader.mjs`、`src/types.ts`、`src/App.tsx`、`src/styles.css`、`tests/reader.test.mjs`。
- 边界：只提取用户可见文本，不把环境说明、图片路径或内部推理当问题；新轮次不沿用旧问题。
- 验证：当次 7 项读取测试与构建通过；浏览器验证子标题、两行限制和当时的搜索；后续已核对原生桌面显示。
- 交付：已纳入桌面应用。旧版曾支持按子标题搜索；当前布局已无搜索入口，此历史记录不代表现在仍提供搜索。
- 回退：移除可选子标题展示不影响任务状态和原始会话。

## BASE-20260914 — 现有能力登记

首次建档时源码已有 F01–F12，包括多来源、实时状态、CLI 分类、提醒、3D 机器人、缩放与历史隐藏。详细入口与限制登记在功能索引。这是代码盘点，不替其他协作任务补签作者、发布日期、测试结果或发布状态；后续行为变化逐条记录。

## 记录模板

```md
## CHG-YYYYMMDD-NN — 具体改动名称

- 功能：Fxx。
- 原因：用户问题或缺陷触发条件。
- 前后变化：原行为 → 新行为；给一个可核验的例子，使用合成内容。
- 涉及文件：仓库相对路径或相对链接。
- 边界：读取/写入对象、状态或统计语义是否改变；必要的迁移与兼容影响。
- 验证：实际执行的命令、结果与范围；未做的检查明确写未做，不引用别的任务结果冒充本次。
- 交付：源已改 / 构建通过 / 包已生成 / 桌面已确认 / 尚待某一步；写明真实状态。
- 回退：可执行的恢复办法或已知限制，不承诺不存在的旧包。
```

新建功能分配新 F 编号；修改已有功能沿用编号。修正文档中的错误时补充原因；未完成条目的验证与交付状态可更新，已交付行为再次改变应新增记录。
