# 变更记录

本文件从 2026-09-14 开始维护。按记录时间倒序追加；日期使用项目本地时间。功能编号见 [功能索引](features.md)。基线盘点不代表新增发布；没有证据的历史不补造。

## CHG-20260917-01 — 修复 Codex 恢复运行后仍显示旧失败状态

- 功能：F02、F05。
- 原因：分页历史索引可能停在失败轮次，而会话日志已写入后续轮次；旧 reader 在存在分页索引时不校验日志，桌面空闲状态也不会纠正该旧失败。
- 前后变化：只采用索引终态 → 同时有界读取日志末尾 192 KiB；明确轮次事件的时间新于索引轮次及活动证据，且轮次或状态已变化时，更新状态及关联字段。恢复运行时清除旧轮次的等待、失败提醒、完成时间和问题摘要，后续明确完成正常进入已完成。
- 涉及文件：`core/codex-reader.mjs`、`tests/reader.test.mjs`、`docs/features.md`、`docs/CHANGELOG.md`。
- 边界：只读原始记录；不凭文件修改时间、普通回复或无时间事件推断成功；不覆盖更新的索引失败；不完整或不可读日志不推翻索引，超出日志尾部且无明确事件时仍依赖索引与实时状态；不更新已发布标签。
- 验证：相关读取、提醒及 Token 测试 21 项通过，覆盖失败后重试、同轮次恢复、后续完成、索引追平后的新失败、陈旧/无时间/不完整日志及长日志截断；构建、源码及打包生成物隐私检查通过。重启后原生窗口确认失败数由 1 变为 0、已完成由 17 变为 18，目标任务已显示本轮完成。
- 交付：源代码随本次修复提交归档并推送至主分支；本地应用包已生成并重启，桌面状态已确认，未发布新版 Release。包内记录为打包时快照，最终验证补记于仓库。
- 回退：恢复 reader 后重新构建；无来源数据或设置迁移。

## CHG-20260916-03 — 合入修正版补丁并标记 1.0.1

- 功能：F03、F05、F08、F11、F12。
- 原因：用户要求提交当前成果并打标签。
- 前后变化：修正版补丁独立交付 → 合入主项目；保留 12 小时和 24 小时隐藏选项；依赖清单及锁文件版本由 1.0.0 同步更新为 1.0.1，设置与后续构建的应用元数据沿用统一版本来源。
- 涉及文件：修正版补丁的来源监听、窗口定位、可选桥接、列表及对应测试和文档；另含历史隐藏选项、`package.json`、`package-lock.json` 和本记录。
- 边界：只提交源码并创建本地 `v1.0.1` 标签，不推送远端、不发布 Release、不安装桥接；没有更新或重启桌面应用。原补丁历史条目的实机记录仅为来源记录，本次不据此宣称已完成真实 Cursor/Kiro 验证。
- 验证：合并后的全量 104 项测试、生产构建、源码隐私检查和差异格式检查通过；确认 12/24 小时选项及对应迁移测试保留。
- 交付：本记录随 1.0.1 源码提交归档，本地附注标签为 `v1.0.1`；现有桌面应用仍为此前打包版本，未生成新版应用包或执行真实窗口联调。
- 回退：可检出 `v1.0.0` 源码重新构建；不移动已有标签，不修改来源任务数据。

## CHG-20260916-02 — 修复补丁中的任务误定位与长日志漏识别

- 功能：F03、F05、F08。
- 原因：补丁审阅复现了 Cursor 标题子串误点、Kiro 同名目录误选窗口，以及用户事件超出日志尾部后真实会话被当成空白标签隐藏。
- 前后变化：Cursor 改为唯一完整标题匹配；Kiro 已知工作区必须完整路径唯一匹配，重复路径也不再降级到名称匹配；Kiro 在缺少轮次边界时有限补读，超过上限或日志不可读时保留未知，仅确认完整空日志后过滤默认标题。
- 涉及文件：`core/app-window-jump.mjs`、`core/kiro-window-bridge.mjs`、`core/kiro-reader.mjs`、三组对应测试、README、功能索引和监听专题。
- 边界：补读最多 2 MiB，仍受整轮 12 MiB 预算限制；孤立工具记录仍不能推断运行，不读取或改写第三方设置，不安装桥接；保留主项目已有的隐藏时间改动。
- 验证：23 项针对性测试和三项原始复现探针通过；全量 103 项测试中 102 项在沙箱通过，唯一受临时 Unix socket 限制的 IPC 用例提权后通过（IPC 3/3）；构建和源码隐私检查通过。修正版和增量 patch 均经过应用验证。
- 交付：在隔离副本修复，准备交付替代原补丁的完整 patch 及仅包含本次修复的增量 patch；未合入主项目、未更新桌面应用或发布。历史条目中的实机验证属于原补丁记录，本次没有复测真实 Cursor/Kiro 窗口。
- 回退：放弃修正版补丁或逆向应用本次增量；来源记录未修改。

## CHG-20260916-01 — 增加 12 小时和 24 小时隐藏选项

- 功能：F11。
- 原因：用户需要更短的历史任务隐藏时间。
- 前后变化：最短为 3 天 → 可选 12 小时、24 小时；24 小时不再被旧迁移规则转换为 3 天，两个新值保存后均正常保留。
- 涉及文件：`core/task-visibility.mjs`、`tests/task-visibility.test.mjs`、`README.md`、`docs/features.md`、`docs/CHANGELOG.md`。
- 边界：默认及现有设置不变；只隐藏历史任务，不删除来源数据，进行中和待处理任务继续显示；不更新已发布版本或标签。
- 验证：相关 8 项测试、构建及源码和打包生成物隐私检查通过；覆盖小时级到期边界、活跃任务保留、合法值序列化及旧值迁移。原生菜单已确认两个新选项，24 小时保存并重启后仍保留，核验后已恢复原有 3 天设置。
- 交付：源代码已改，应用包已生成并重启，桌面设置已核对；未发布新版。包内记录为打包时快照，最终核对结果补记于仓库。
- 回退：恢复选项及迁移规则后重新构建；无来源任务数据迁移。

## CHG-20260915-13 — 兼容 Cursor 新版 bubble 运行生命周期

- 功能：F03、F05。
- 原因：CHG-20260915-12 只提高了 `generatingBubbleIds` 的优先级，但当前 Cursor Agents 实机续聊时该数组仍为空，父会话还会残留上一轮的 `completed` 或 `aborted`，导致运行中的任务继续显示为已完成或中断。
- 前后变化：只读取父会话状态与生成数组 → 额外只读最后一个公开 conversation header 对应 bubble 的生命周期标量；assistant bubble 有开始时间、没有自身结束字段，且开始时间新于父会话更新时间时，证明当前活动尚未被父记录吸收，并用该开始时间刷新陈旧判断。父会话时间追平或 bubble 出现结束字段后回到父会话终态。
- 涉及文件：`core/cursor-reader.mjs`、`tests/cursor-reader.test.mjs`、`docs/features.md`、`docs/multi-provider-monitoring.md`、`docs/CHANGELOG.md`。
- 边界：只查询最新 bubble 的类型和时间字段，不读取其文本、思考、工具结果或不透明会话状态；最终文本 bubble 即使没有自身结束字段，只要父记录时间已追平就不误报运行；没有开始时间不猜运行，超过统一陈旧阈值仍显示待核实；不修改 Cursor 数据、窗口或配置。
- 验证：已在同一运行时刻确认 Cursor 窗口存在停止控件、Aster 旧 reader 错误显示完成，且最新 assistant bubble 的开始时间新于父记录、没有结束字段；该轮结束后父记录时间追平且 Cursor 显示 Completed。首次只检查 bubble 结束字段的候选实现会在桌面误报运行，已在交付前收紧并增加父记录追平用例；最终 Cursor 专项 8 项、Node 24 全量 96 项、生产构建、源码及生成物隐私检查和差异格式检查均通过。
- 交付：源代码、生产构建和应用包已生成；Aster 已重启加载新 reader 并恢复本机任务快照。现场捕获覆盖了修复依据所需的运行/结束前后状态。
- 回退：移除最新 bubble 生命周期查询并重新构建；Cursor 原始记录不受影响。


## CHG-20260915-12 — Cursor 续聊优先识别当前生成状态

- 功能：F03、F05。
- 原因：Cursor Agents 在已完成会话中开始新一轮时，可能先写入当前生成项，同时暂时保留上一轮的 `completed`；reader 过去先采用结束状态，导致正在工作的续聊仍显示在“已完成”。
- 前后变化：结束状态覆盖当前生成项 → 非空的当前生成项优先判定为进行中；没有生成项时仍按明确完成、失败、中断或等待状态处理，长时间不更新的生成记录仍降为待核实。
- 涉及文件：`core/cursor-reader.mjs`、`tests/cursor-reader.test.mjs`、`docs/features.md`、`docs/multi-provider-monitoring.md`、`docs/CHANGELOG.md`。
- 边界：只调整同一 Cursor IDE 记录内两个显式字段冲突时的优先级；不读取窗口文案推断状态，不修改 Cursor 数据，不影响 CLI、其他来源或任务定位。
- 验证：Cursor reader 专项 8 项与 Node 24 全量 96 项测试通过，生产构建、源码及生成物隐私检查、差异格式检查通过；沙箱内首次全量测试仅因临时 Unix socket 权限受限失败，允许该权限后同一套测试全部通过。实机报告中的运行轮次已在验证前结束，因此新包的实时续聊状态仍需下一次运行轮次确认。
- 交付：源代码、生产构建和应用包已生成，Aster 已重启并恢复本机任务快照；后续实机续聊证明当前 Cursor 版本不会可靠填充该生成数组，因此本条不足以单独修复问题，后续兼容见 CHG-20260915-13。
- 回退：恢复 Cursor 状态解析中结束状态优先级并重新构建；来源记录不受影响。

## CHG-20260915-11 — Cursor/Kiro 只定位已打开的原窗口

- 功能：F03、F08、F12。
- 原因：通过应用 URL 或项目路径定位会把请求交给系统启动服务，可能新开 Cursor/Kiro 软件或窗口；Kiro 记录中的项目名也不一定等于当前展示该会话标签的工作区名。
- 前后变化：点击可能调用 URL 并新开应用/窗口 → Cursor 只在已运行的 `Cursor Agents` 窗口中通过辅助功能选择唯一同名任务；Kiro 由每个现有窗口的可选桥接发布工作区和窗口提示，Aster 按原生会话 ID 与工作区选择唯一窗口、请求固定的切换/查看命令，再通过辅助功能置前该原窗口。应用没运行、目标没打开、窗口不唯一、权限或桥接缺失时直接失败，不回退到 URL、项目打开或应用启动。
- 涉及文件：`core/app-window-jump.mjs`、`core/kiro-window-bridge.mjs`、`core/cursor-reader.mjs`、`core/kiro-reader.mjs`、`integrations/kiro-bridge/`、`electron/main.mjs`、`scripts/package.mjs`、对应测试、README 与专题文档。
- 边界：主进程先从当前快照校验来源 ID；Kiro 桥接仅监听回环地址、使用随机令牌并接受固定的合法会话 ID，不读取对话、不发送提示、不创建任务、不修改 Kiro 设置或启动入口。安装或更新桥接需要重启一次 Kiro 使扩展加载；之后 Aster 重启、未启动或被移除均不影响 Kiro，桥接缺失仅让精确定位明确失败。Cursor/Kiro 定位都不调用应用 URL、启动服务或新建窗口。
- 验证：Node 24 全量 96 项、生产构建、源码 82 个及含生成物 173 个文本隐私检查、`git diff --check` 均通过。新包原生列表显示 12 个证据支持的任务且没有空白 Kiro 会话；Cursor 点击在原 Agents 窗口选中目标任务，Kiro 在两个不同工作区间双向点击均选中目标会话并置前对应原窗口，前后既有 Cursor/Kiro 主进程未变化。Kiro 旧会话在点击并等待同步周期后仍为“状态未知”，未误报进行中。
- 交付：源代码、构建和应用包已生成；Aster Kiro Bridge 0.3.4 已安装并在两个既有窗口加载，最终 release 已启动确认。旧试验 VSIX 已从 release 清除。
- 回退：卸载或停用 Aster Kiro Bridge 并恢复旧跳转实现后重新构建；只会失去 Kiro 精确定位，Kiro 会话、配置及后续流程不受影响。

## CHG-20260915-10 — Cursor 任务精确定位

- 功能：F03、F08、F12。
- 原因：Cursor 任务过去只执行应用激活，没有携带 composer ID，因此点击后不会定位列表中选择的原任务。
- 前后变化：点击 Cursor 行仅激活当前窗口 → 安装 Aster Cursor Bridge 后，只对合法 UUID composer ID 显示“打开 Cursor 任务”；受限 `cursor://` URI 先按项目名转发到已经打开的匹配工作区，再调用固定的 `composer.openComposer`。找不到原工作区时明确失败，不在当前窗口打开、不新建窗口；未安装扩展、CLI 记录或非法 ID 不显示入口。
- 涉及文件：`integrations/cursor-bridge/`、`core/cursor-bridge.mjs`、`core/cursor-reader.mjs`、`electron/main.mjs`、`scripts/package-cursor-bridge.mjs`、`scripts/package.mjs`、`scripts/check-privacy.mjs`、对应测试与文档。
- 边界：扩展不读取对话内容，不发送提示，不创建任务，不打开项目或窗口，不修改 Cursor 设置；只接受 UUID 与安全项目名，并调用固定的窗口路由及查看命令。这些命令不是 Cursor 公共稳定 API，版本不兼容时明确报错。
- 验证：0.1.0 能选中标题但会落入新开的 Agents 窗口，不满足原窗口定位，已判定失败并由 0.2.0 的已有工作区路由替代。0.2.0 Cursor reader、桥接、跳转与隐私专项 15 项及 Node 24 全量 93 项测试通过，生产构建、源码 79 个文本隐私检查与 `git diff --check` 通过；待桌面验证。
- 交付：0.1.0 不作为完成交付；待生成并安装 0.2.0、验证不新增窗口后重新打包。
- 回退：从 Cursor 卸载 `Aster Cursor Bridge` 并恢复 Cursor reader 跳转目标后重新构建；Cursor 任务与配置不受影响。

## CHG-20260915-09 — Kiro 查看会话不再误报进行中

- 功能：F03、F05、F08。
- 原因：Kiro 在重启、恢复或查看旧会话时会追加没有新用户请求的工具记录；reader 过去把孤立的 assistant/tool_call 当作新一轮活动，导致点击跳转后多个空闲任务变成“进行中”。
- 前后变化：孤立 assistant/tool_call 可凭自身创建运行轮次 → 只有在当前有已观察到的 user 事件所开启轮次时，assistant/tool_call 才延续活动；已完成轮次后的工具记录保持完成，完全孤立的记录保持状态未知。文件 mtime 不再覆盖已有生命周期事件时间。
- 涉及文件：`core/kiro-reader.mjs`、`tests/kiro-reader.test.mjs`、`docs/features.md`、`docs/CHANGELOG.md`。
- 边界：不把没有用户事件的尾部片段猜成运行中；超大日志若用户事件已离开有界读取范围，会保守显示待核实/未知，不修改 Kiro 会话记录或跳转能力。
- 验证：Kiro reader 与桥接专项 13 项测试通过；Node 24 全量 91 项测试通过，生产构建通过，源码 73 个文本隐私检查通过，`git diff --check` 通过。release 桌面实测中，空 Kiro 会话与旧会话均保持“状态未知”；点击旧会话后 Kiro 选中原会话，等待一次同步周期后 Aster 仍保持“状态未知”，未转为进行中。
- 交付：源代码已修改且验证通过；release 应用包已生成并已启动确认。
- 回退：恢复孤立工具事件创建运行轮次和文件 mtime 优先级后重新构建；来源记录不受影响。

## CHG-20260915-08 — 增加 Kiro 原会话桥接扩展

- 功能：F03、F08、F12。
- 原因：Kiro 应用激活不能定位列表中选定的原会话，而当前版本没有供外部程序直接调用本地会话 ID 的公开入口。
- 前后变化：Kiro 任务始终不可点击 → 安装 Aster Kiro Bridge 后，Aster 对合法的 IDE/本地会话显示“打开 Kiro 任务”，通过受限 `kiro://` URI 让扩展调用固定的 `kiroAgent.viewSession`；未安装扩展、CLI 记录或非法 ID 仍不可点击。
- 涉及文件：`integrations/kiro-bridge/`、`core/kiro-bridge.mjs`、`core/kiro-reader.mjs`、`electron/main.mjs`、`scripts/package-kiro-bridge.mjs`、`scripts/package.mjs`、`scripts/check-privacy.mjs`、对应测试与文档。
- 边界：扩展不读取对话内容，不执行提示或任意命令，不创建会话，不修改 Kiro 配置；只接受 `sess_` token 或 UUID，并调用一个固定内部查看命令。该命令不是 Kiro 公共稳定 API，版本不兼容时明确报错。
- 验证：Kiro 桥接、reader、聚合与跳转专项 20 项测试通过；Node 24 全量 90 项测试通过，生产构建通过，源码 73 个及含生成物 153 个文本隐私检查通过；VSIX 结构有效且 Kiro CLI 安装成功，Aster 运行时已检测到扩展。正常重启 Kiro 后，从 Aster 点击一个标题明确的 Kiro 任务，Kiro 激活包含该任务的原工作区窗口并选中同名原会话；未新建项目窗口。首次 URI 由 Kiro 显示一次性确认，验证时未勾选永久允许。
- 交付：扩展 VSIX 已生成并安装，Aster 应用包已生成并启动；Kiro 已重启并加载桥接，原生跳转确认通过。
- 回退：从 Kiro 卸载 `Aster Kiro Bridge` 并恢复 Kiro reader 跳转目标后重新构建；Kiro 会话与配置不受影响。

## CHG-20260915-07 — 恢复 Codex 任务跳转入口

- 功能：F01、F08。
- 原因：Kiro 非交互行按 `jumpTarget` 统一判断后，Codex 原生任务虽然由主进程使用应用内深链定位，却没有显式展示目标，因而被界面误判为不可跳转。
- 前后变化：Codex 原生任务显示“不可直达” → 聚合层为有效的 Codex 原生任务 ID 标注应用内跳转能力；Aster 包装的进程观察仍只使用自身可验证的终端目标。
- 涉及文件：`core/all-readers.mjs`、`tests/all-readers.test.mjs`、`docs/features.md`、`docs/CHANGELOG.md`。
- 边界：只恢复 Codex 原生任务已有的 `codex://threads/` 路径，不向渲染层暴露任意 URL；Kiro 保持不可点击，其他来源不变。
- 验证：聚合、Kiro 与跳转专项 17 项测试通过；Node 24 全量 87 项测试通过，生产构建通过，源码 67 个文本隐私检查通过。新包原生列表确认 Codex 原生任务恢复为“打开 Codex 任务”按钮，Kiro 仍为“不可直达”非交互行；实际点击 Codex 行后列表失焦收起，目标 Codex 界面受自动化安全限制未读取其内容。
- 交付：源代码已修改、构建及应用包已生成；桌面进程已加载修改后的版本。
- 回退：移除聚合层的 Codex 展示目标；来源记录不受影响。

## CHG-20260915-06 — 取消 Kiro 误导性跳转

- 功能：F03、F08。
- 原因：激活 Kiro 只能打开当前窗口，不能定位列表中选定的原任务；继续显示跳转箭头会造成已直达目标的错误预期。
- 前后变化：Kiro IDE/本地任务可点击并激活应用 → Kiro 任务仍正常展示，但使用非交互行并标注“不可直达”；目标校验也拒绝外部构造的 Kiro 应用跳转。
- 涉及文件：`core/kiro-reader.mjs`、`core/task-jump.mjs`、`src/App.tsx`、`src/styles.css`、对应测试、`README.md`、`docs/features.md`、`docs/CHANGELOG.md`。
- 边界：不修改、删除或接管 Kiro 会话；Kiro 当前没有受支持的本地会话 ID 外部入口。Cursor、终端及其他已有跳转能力不受影响。
- 验证：Kiro reader 与跳转 13 项测试通过；Node 24 全量 86 项测试通过，生产构建通过，源码 67 个文本隐私检查通过；沙箱内全量测试仅因临时 Unix socket 权限受限失败，允许该权限后同一套测试全部通过。新包已在原生桌面显示 12 个任务，Kiro 行由按钮变为带“不可直达”提示的非交互容器，仍支持的 Cursor 行保持按钮。
- 交付：源代码已修改、构建及应用包已生成；桌面进程已加载修改后的版本。
- 回退：恢复 Kiro 应用目标及交互行后重新构建；第三方记录不受影响。

## CHG-20260915-05 — 列表只保留真正的 Claude Code 任务

- 功能：F01、F03。
- 原因：Claude SDK 产生的 `sdk-cli` 单次调用记录被误归类为普通 Claude Code CLI 会话，造成列表出现大量标题相近的伪任务，并把真正任务挤出最近记录范围。
- 前后变化：所有顶层 JSONL 会话都进入列表 → 明确排除入口为 `sdk-cli` 的 SDK 调用；读取最近 `limit` 个文件后过滤 → 在有界范围内多扫描候选，再过滤并保留真正任务。
- 涉及文件：`core/claude-reader.mjs`、`tests/claude-reader.test.mjs`、`README.md`、`docs/features.md`、`docs/CHANGELOG.md`。
- 边界：只改变 Aster 的只读展示，不删除或修改 Claude 记录；普通 CLI、IDE、活进程与 Aster 包装任务仍按原规则保留；未知入口不会仅凭猜测过滤。
- 验证：Claude reader、聚合与历史可见性相关 17 项测试通过；Node 24 全量 86 项测试通过，生产构建通过，源码 67 个及含生成物 142 个文本隐私检查通过。重新打包后在原生桌面确认列表由 54 项降为 12 项，且不再出现 Claude SDK 单次调用。
- 交付：源代码已修改、构建及应用包已生成；桌面进程已加载过滤后的版本。
- 回退：恢复 `sdk-cli` 过滤和候选扩展后重新构建；来源记录不受影响。

## CHG-20260915-04 — 修复任务列表点击并避免 Cursor/Kiro 新开窗口

- 功能：F03、F08。
- 原因：桌宠点击打开列表时会在同一指针事件内抢焦点并立刻失焦隐藏；Cursor/Kiro 跳转把项目路径交给应用后会打开新窗口，而不是返回原有状态。
- 前后变化：列表同步显示并聚焦 → 先无激活显示，待当前点击结束后再聚焦；Cursor/Kiro 按项目路径打开 → IDE/本地任务只激活应用且不传项目路径，CLI 记录不再错误跳到 GUI；名称解析失败时仍从 macOS 系统 API 返回的应用目录查找白名单应用包并重试。
- 涉及文件：`core/list-window.mjs`、`core/task-jump.mjs`、`electron/main.mjs`、对应测试、`README.md`、`docs/features.md`、`docs/CHANGELOG.md`。
- 边界：列表仍在点击外部、再次点击或按 Escape 时收起；跳转仍只由用户点击触发。Cursor/Kiro 当前版本没有可供外部程序按本地会话 ID 直达原会话的入口，因此这里只返回应用现有状态，不把激活应用冒充为精确会话定位，也不修改第三方配置或输入命令。
- 验证：Cursor/Kiro reader、列表与跳转相关 22 项测试通过；Node 24 全量 85 项测试通过，生产构建通过，源码 67 个及含生成物 142 个文本隐私检查通过。新包已在原生桌面启动并显示任务列表；跳转测试确认传给 Cursor/Kiro 的启动参数不含项目路径。由于系统未授予辅助功能权限，未自动读取第三方应用窗口数量；此前“打开对应项目和任务”的结论已由用户反馈证伪，不再作为证据。
- 交付：源代码已改、构建通过、应用包已重新生成并启动；当前进程已加载新包。
- 回退：恢复列表同步聚焦与仅按应用名启动后重新构建；用户任务记录和第三方应用不发生迁移。



## CHG-20260915-03 — 从 1.0.0 开始发布正式版本

- 功能：F12。
- 原因：用户指定从 1.0.0 开始编号，并发布 Release 与 Git 标签。
- 前后变化：0.1.0 开发版本 → 1.0.0；设置、应用元数据与锁文件同步，发行包使用版本化文件名并提供 SHA-256 校验值。
- 涉及文件：`package.json`、`package-lock.json`、`docs/CHANGELOG.md`，以及由构建脚本生成的发行物；本次提交同时收录设置版本展示和统一打包版本来源的改动。
- 边界：发布到当前项目仓库，标签为 `v1.0.0`；仅提供 macOS Apple Silicon 构建，未进行 Apple 签名或公证，不包含用户数据。
- 验证：构建及 81 项测试通过，源码和打包生成物隐私检查通过；发布附件提供 SHA-256 校验值，发布前核对应用元数据与包内版本。
- 交付：本记录随 v1.0.0 发行源码归档；发布结果以仓库 Release 和远端标签为准。
- 回退：可检出此前源码重新构建；不移动或覆盖已发布标签。


## CHG-20260915-02 — 设置展示统一版本号

- 功能：F12。
- 原因：用户需要可见的应用版本号。
- 前后变化：版本仅记录在依赖清单且打包脚本重复硬编码 → 设置顶部显示当前版本，前端与打包脚本统一读取 `package.json` 的 `version`，沿用现有 0.1.0。
- 涉及文件：`src/App.tsx`、`scripts/package.mjs`、`README.md`、`docs/features.md`、`docs/CHANGELOG.md`。
- 边界：只读版本展示，不改变用户设置、任务监测或更新机制；修改版本后需重新构建与打包。
- 验证：构建、源及生成物隐私检查通过；源码、暂存与归档清单、macOS 应用版本和构建版本均为 0.1.0；原生设置页已确认显示当前版本。
- 交付：应用包已生成并重启，版本显示已实机确认；包内记录为打包时快照，最终核对结果补记于仓库。
- 回退：移除版本展示并恢复打包脚本即可，无用户数据迁移。


## CHG-20260915-01 — 建立源码版本基线

- 功能：F12。
- 原因：用户要求提交当前项目，目录此前没有 Git 版本记录。
- 前后变化：未纳入版本控制 → 初始化本地仓库并提交当前源码、测试、维护文档与运行/打包所需图标；构建目录仅保留两个必需图标，排除依赖、应用包与 QA 预览。
- 涉及文件：`.gitignore`、本变更记录；首次提交收录项目现有源码。
- 边界：只创建本地提交，不建立远程仓库或上传；不更改应用行为或用户设置，不删除已生成的本地文件。
- 验证：源码隐私检查通过，构建通过，现有测试 81/81 通过。
- 交付：纳入本次本地首次提交；未执行远程推送。
- 回退：版本控制不改变当前应用；生成物继续由构建脚本重建。


## CHG-20260914-06 — 右上角默认位置、位置记忆与开机启动

- 功能：F10、F13。
- 原因：用户希望首次位于右上角，拖动后保留位置，并可选择登录时启动。
- 前后变化：每次固定右下角 → 首次右上角、移动后自动保存显示器及坐标；重启恢复位置，显示器重排按相对位置恢复、断开时回到主屏右上角。设置新增开机启动开关，默认不注册。
- 涉及文件：`core/pet-position.mjs`、`core/login-startup.mjs`、`electron/main.mjs`、`src/App.tsx`、`src/types.ts`，对应测试与使用说明。
- 边界：位置仅保存到 Aster 自有数据目录；登录项只在用户切换时修改 Aster 自身，实际状态从系统读取。开发运行时禁用注册。原任务和其他软件配置不变；不执行真实注销/重启电脑测试。
- 验证：位置、缩放与登录项适配共 12 项测试通过，覆盖旧值、序列化恢复、负坐标显示器、断屏、启动开关往返、系统修改和失败；构建通过。隔离的隐藏 Electron 窗口验证了默认右上角、移动事件持久化和重启位置恢复；实际应用设置页确认开机启动开关可用且默认关闭。未注销电脑测试自动登录启动，也未为测试修改用户系统登录项。
- 交付：源代码已改、构建通过，应用已打包并重启；原生设置入口及隔离原生位置往返验证通过。
- 回退：设置中关闭开机启动；位置记忆不改变任务记录。回退源改动后重新构建，无独立已验证旧包。


## CHG-20260914-06 — 清理初版角色与失效样式

- 功能：F06、F09、F12。
- 原因：当前已使用机器人界面，初版角色加载、导入存储及旧布局样式不再有入口。
- 前后变化：删除旧角色加载组件、IndexedDB 存储封装、附带模型及专用依赖；161 条样式规则/568 项声明 → 136 条规则/446 项声明，清除旧布局规则与被覆盖声明，并整理格式。
- 涉及文件：移除 `src/Avatar.tsx`、`src/model-store.ts`、`public/models/companion.vrm`；更新 `src/styles.css`、`src/App.tsx`、`src/types.ts`、`electron/main.mjs`、`electron/preload.cjs`、依赖清单与锁文件、第三方说明和功能索引；移除图标脚本未使用的导入。
- 边界：当前 3D Robot、SVG 回退及动作仍保留；不删除用户已保存的模型数据，不修改任务来源记录。删除没有发送方的旧模式订阅和没有调用方的模型刷新分支，不改变现有任务/设置 IPC。已移除模型与库的分发署名同步删除，其余仍用依赖的许可证保留。
- 验证：380×490 合成任务预览中，任务列表 204、设置 58、桌宠 70 个元素清理前后计算样式完全相同；构建通过。全量测试 72 项通过，唯一受沙箱监听限制的 IPC 用例在允许监听的环境补跑通过（IPC 共 3/3），全量 73 个用例均已获得通过结果。
- 交付：源代码已清理，统一生成的应用包已包含本次清理；归档和暂存区均无旧模型及专用依赖，包内前端资源与构建结果一致，废弃桥接已移除。生成物隐私检查 132 个文本通过，维护文档链接可解析。打包负责人已重启并完成原生显示核对，本任务未重复打包或重启。包内文档为打包时快照，最终结果以仓库记录为准。
- 回退：恢复独立模块后须重新安装对应依赖并构建；本次不迁移或清除旧 IndexedDB 数据。没有建立独立回退包，不承诺一键恢复。


## CHG-20260914-05 — 机器人增加轻快节奏与间歇招手

- 功能：F09。
- 原因：用户希望机器人表现更欢快。
- 前后变化：平缓漂浮和工作摆臂 → 待命/工作/完成增加双拍起伏与头身反向轻摆、微笑加深；待命招手间隔缩短，工作每 8 秒短暂抬手；完成时短暂连续弹跳和双臂欢呼。
- 涉及文件：`core/robot-motion.mjs`、`src/robot.css`、`tests/robot-motion.test.mjs`、`docs/features.md`、`docs/CHANGELOG.md`。
- 边界：不改变任务状态判定、数量或提醒；失败等状态仍保留原语义。减少动作时保持静态。SVG 回退同步欢快节奏，不增加模型资源或外部调用。
- 验证：动作相关 6 项测试通过，覆盖完整动作周期的视口边界、状态切换平滑、欢呼结束、间歇招手及减少动作；构建和打包隐私检查通过。
- 交付：代码和构建已完成；应用包已生成并重启；原生窗口已确认新版工作状态机器人正常显示、身体与状态条未裁切。包内记录为打包时快照，最终核对结果补记于仓库。
- 回退：恢复这三个动画与测试文件的对应调整后重新构建；无独立已验证回退包。


## CHG-20260914-04 — 刷新分发文档并保留项目规则入口

- 功能：F12。
- 原因：源码说明清理后需同步生成副本，README 新增的项目规则链接也需要在包内有目标文件。
- 前后变化：旧暂存和应用说明 → 从当前源重新构建；分发缺少项目规则文件 → 打包时一并复制 `AGENTS.md`。
- 涉及文件：`scripts/package.mjs`、`docs/features.md`、`docs/CHANGELOG.md`，以及脚本生成的构建与应用副本。
- 边界：只刷新本项目分发物；不恢复旧路径示例，不修改其他软件配置。开发索引的源码和测试链接需在源码仓库查看，应用不附带开发源码和测试。
- 验证：构建、打包前后隐私检查通过；显式生成物检查 132 个文本通过。包内存在 README 引用的项目规则与维护文档。
- 交付：应用包已生成；本次文档与分发清理没有再次重启运行中的桌宠。
- 回退：通过源文件与打包脚本重新生成；不恢复已清理的个人信息或固定路径。

## CHG-20260914-03 — 维护规则与路径隐私边界

- 功能：F12，涉及所有模块的维护约定。
- 原因：需要明确每次修改可做什么、如何定位功能、如何记录结果，并防止个人信息或固定机器路径进入项目与交付物。
- 前后变化：分散的使用说明 → 项目协作入口、维护规范、功能索引和可追溯变更记录；机器专属 CLI 示例 → 项目相对入口；固定系统命令位置 → 按名称调用；固定测试目录 → 运行时临时目录。
- 涉及文件：`AGENTS.md`、`README.md`、`docs/`，`scripts/check-privacy.mjs`、`scripts/package.mjs`、`package.json`、`index.html`，`core/local-reader-utils.mjs`、`core/task-jump.mjs` 及对应测试。
- 边界：只清理 Aster 源与由其生成的交付物，不修改其他软件数据；不在本记录复述被清理的值。API 路由、设备格式、标准解释器声明不作为安装目录。原始运行时记录仍仅在本机用于任务观察。
- 验证：源文本检查 63 个文件通过；读取、CLI、跳转相关 31 项测试与新增隐私检查 2 项通过；构建通过；6 份文档的 59 个本地链接均可解析。生成物初次检查发现旧副本；统一打包后复查通过，包含构建目录、暂存副本与应用包内文本。
- 交付：代码与文档已写入；统一打包负责人已从清理后的源刷新应用包，打包前后隐私检查通过。本条结果随后同步到仓库记录；包内文档是打包时的静态快照，以仓库最新记录为准。未为文档更新重启应用。
- 回退：维护文档可按记录修订；不要恢复被清理的信息。工具按名称调用时依赖运行环境可发现对应系统工具；不可用时走现有失败提示。没有为本条生成独立回退包。

## CHG-20260914-02 — 每个任务展示 Token 用量

- 功能：F07。
- 原因：用户需要看每个任务的 Token 消耗。
- 前后变化：卡片无用量 → 展示累计或近期 Token；悬停查看精确值；数据缺失显示未提供。
- 涉及文件：`core/token-usage.mjs`、Codex/Claude reader、`core/local-reader-utils.mjs`、`src/types.ts`、`src/App.tsx`、`src/styles.css`、`tests/token-usage.test.mjs`。
- 边界：Codex 累计字段与日志累计值相符；Claude 按消息 ID 去重，截断日志标近期；缓存包含在统计内，非费用或额度。其他来源没有记录时不虚构用量。
- 验证：当次相关 16 项测试与构建通过；已在桌面应用的任务卡片确认累计 Token 显示。具体运行值不写入维护样例。
- 交付：当次已纳入统一应用包并确认桌面显示。后续卡片布局调整以当前源码为准。
- 回退：可移除展示与可选字段读取，不更改来源统计记录；不要把缺失用量改成 0。

## CHG-20260914-01 — 主题下增加具体问题子标题

- 功能：F06。
- 原因：任务主题不足以说明本轮具体问题。
- 前后变化：只有主题 → 主题下显示当前轮次可读取的用户问题，缺内容不占空行，长文本限制两行。
- 涉及文件：`core/user-request.mjs`、`core/codex-reader.mjs`、`src/types.ts`、`src/App.tsx`、`src/styles.css`、`tests/reader.test.mjs`。
- 边界：只提取用户可见文本，不把环境说明、图片路径或内部推理当问题；新轮次不沿用旧问题。
- 验证：当次 7 项读取测试与构建通过；浏览器验证子标题、两行限制和当时的搜索；后续已核对原生桌面显示。
- 交付：已纳入桌面应用。旧版曾支持按子标题搜索；当前布局已无搜索入口，此历史记录不代表现在仍提供搜索。
- 回退：移除可选子标题展示不影响任务状态和原始会话。

## BASE-20260914 — 现有能力登记

首次建档时源码已有 F01–F12，包括多来源、实时状态、CLI 分类、提醒、3D 机器人、缩放与历史隐藏。详细入口与限制登记在功能索引。这是代码盘点，不替其他协作任务补签作者、发布日期、测试结果或发布状态；后续行为变化逐条记录。

## 记录模板

```md
## CHG-YYYYMMDD-NN — 具体改动名称

- 功能：Fxx。
- 原因：用户问题或缺陷触发条件。
- 前后变化：原行为 → 新行为；给一个可核验的例子，使用合成内容。
- 涉及文件：仓库相对路径或相对链接。
- 边界：读取/写入对象、状态或统计语义是否改变；必要的迁移与兼容影响。
- 验证：实际执行的命令、结果与范围；未做的检查明确写未做，不引用别的任务结果冒充本次。
- 交付：源已改 / 构建通过 / 包已生成 / 桌面已确认 / 尚待某一步；写明真实状态。
- 回退：可执行的恢复办法或已知限制，不承诺不存在的旧包。
```

新建功能分配新 F 编号；修改已有功能沿用编号。修正文档中的错误时补充原因；未完成条目的验证与交付状态可更新，已交付行为再次改变应新增记录。
