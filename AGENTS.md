# Aster 项目协作规则

适用于本仓库。用户当前明确要求优先；本文件细化项目维护方式，不替代工具权限。

## 每次修改

1. 从 [功能索引](docs/features.md) 找到对应功能编号，读相关代码与 [维护规范](docs/maintenance.md)。确认本次要改什么、影响哪些模块、如何验证；已有授权内的常规修改直接推进，不增加逐步审批。
2. 只改实现目标所需的源文件，保留其他任务的改动。多任务同时工作时，指定一个打包与重启负责人；不要同时覆盖共享文件或生成目录。
3. 按维护规范运行相关验证。纯文档修改检查事实、链接与隐私即可；不为其强行运行全套行为测试。
4. 每个独立改动在 [变更记录](docs/CHANGELOG.md) 增加一条，至少记录功能编号、原因、前后行为、涉及文件、边界影响、验证证据和交付状态。未通过或未执行的检查必须如实记录。
5. 行为、入口或限制变化时更新功能索引；操作方式变化时更新 README 或专题文档。记录不得只写“优化”“修复完成”。
6. 交付时区分源代码已改、构建通过、应用包已生成、桌面运行已确认。不得用浏览器预览通过代替桌面更新证据。

## 不可越过的边界

- 不在源码、注释、文档、测试样例、日志样例或分发物中写入个人身份、真实会话内容或机器专属路径。也不要把清理掉的值复制进变更记录。
- 文件位置通过模块位置、当前工作目录、系统目录 API 或配置在运行时取得；文档使用相对入口，测试使用临时目录。禁止硬编码安装位置和用户目录。
- 本机观察所必需的 cwd、TTY 等运行时数据只用于本地定位，不复制进项目样例或对外发布物。
- 第三方记录只读，不批准、恢复、接管任务；不改写其他软件的配置、hooks、启动入口或 shell 环境。移除 Aster 不得导致其他软件不可用。
- 只把明确证据标为完成、失败或等待。Token 缺失不是 0，日志片段不是完整累计，CLI 进程不是会话轮次，隐藏不是删除。
- 不直接编辑 dist、构建暂存区、打包应用。修源文件后通过构建脚本生成；仅在本次明确授权的范围内更新或清理交付物。

提交交付前运行 `npm run check:privacy`；打包脚本会检查源文本及生成物。自动检查不覆盖图片中的文字，截图与其他二进制附件仍须人工确认后才可纳入交付。
